import 'package:flutter/material.dart';

class OperatorDetailPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Operator Detail'),
      ),
      body: Center(
        child: Text('Details of the Operator'),
      ),
    );
  }
}
