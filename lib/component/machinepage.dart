// formation_de_couture_page.dart
import 'package:flutter/material.dart';

class Machinepage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Machine Page'),
      ),
      body: Center(
        child: Text('Details machine'),
      ),
    );
  }
}
